export default function AnalyticsPage() {
  return (
    <div className="p-6 bg-gray-100 dark:bg-gray-900 text-gray-900 dark:text-gray-100 min-h-screen">
      <h1 className="text-2xl font-bold">Analytics</h1>
      <p className="mt-4">Usage metrics (per tenant, per agent, per integration).</p>
      <p className="mt-2">Performance, cost, and operational dashboards.</p>
    </div>
  );
}
