{{/* Call the common service template for the UI component */}}
{{- include "agentforge.component.service" (dict
    "Chart" .Chart
    "Release" .Release
    "Values" .Values
    "componentName" "ui"
    "componentValues" .Values.agentforgeUi
  ) | nindent 0 }}
